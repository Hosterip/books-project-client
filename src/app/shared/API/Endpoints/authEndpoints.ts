const defaultEndpoint = "auth/"

export const authEndpoints = {
    register: defaultEndpoint + "register",
    login: defaultEndpoint + "login",
    logout: defaultEndpoint + "logout",
    changePassword: defaultEndpoint + "change",
}
